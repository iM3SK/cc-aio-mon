## What does this change?

<!-- Brief description of the change and why it's needed -->

## Checklist

- [ ] `python tests.py` passes (all tests green)
- [ ] Both files compile: `python -c "import py_compile; py_compile.compile('statusline.py', doraise=True); py_compile.compile('monitor.py', doraise=True)"`
- [ ] Tested manually with a live Claude Code session
- [ ] Zero new dependencies introduced
- [ ] `MAX_FILE_SIZE` and ANSI palette kept in sync between both files (if changed)
- [ ] CHANGELOG.md updated

## Related issues

<!-- Closes #, Fixes # -->
